import { rem } from '@mantine/core';
const Logo = ({ ...rest }) => (
    <img
        width={120}
        style={{ position: 'relative', marginTop: '-18px' }}
        src="/public/wordlock-og-Logo.png"
    />
);

export default Logo;
